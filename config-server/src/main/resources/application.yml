spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rooanlee/SpringCloudDemo.git
          search-paths: config-repo
          username: rooanlee
          password: wj173749
server:
  port: 8888
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/